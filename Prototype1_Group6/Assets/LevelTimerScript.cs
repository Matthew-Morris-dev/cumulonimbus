using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LevelTimerScript : MonoBehaviour
{
    //STILL TO DO LIST: Level timer w/ Sun and moon, finalising player correct/incorrect answers, Parallax background script. Finish on Friday
    //In charge of how long the level lasts AND (probably) for rotating the sun/moon around
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
